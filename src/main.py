from game_setup import create_setup, initial_state
from pygame_setup import game_loop

if (__name__ == "__main__"):
    # algorithm, difficulty, color = create_setup()
    # initial_state(algorithm, difficulty, color)
    game_loop()